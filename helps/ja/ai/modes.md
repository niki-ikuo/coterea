---
title: Ask / Edit / Agent
keywords:
  - Ask
  - Edit
  - Agent
  - モード
category: ai
related:
  - chat.md
  - proposals.md
commands:
  - Focus Chat
---

# Ask / Edit / Agent

送信前にモードを選びます。スレッドごとに最後に使ったモードを覚えます。

- **Ask**: 添付したファイルや選択範囲について、説明・要約・質問に答えます。未添付ならいま開いているタブを使います。ファイルは書き換えません。
- **Edit**: 添付したファイルへの変更案を1つ出します。未添付ならいま開いているタブが対象です。提案カードの適用で文書に入ります。変更のあとに、何を変える提案かを短く文章で報告します。
- **Agent**: 添付があればその本文を、無ければいま開いているタブの本文を渡します。他に開いているタブは一覧（名前・文字数）だけ添え、本文は `list_open_tabs` / `read_tab` で必要なときだけ読みます。長い文書は `read_tab` の範囲指定（`from` / `to`）を使います。複数の依頼や長めの作業は `update_todo` でタスクに分け、順番に進めます。ステップ数は設定で上限を決められます。

## コンテキストの添付

次のいずれかで、メッセージ文の途中にカプセル（例: `≡ bin/get-weather.sh:6-10`）を挿入できます。

- ファイルタブを入力欄のカーソル位置へドラッグ＆ドロップ（またはタブの右クリック「チャットに追加」）
- エディタで行を選択して入力欄へドラッグ（または選択範囲の右クリック「チャットに追加」）
- 右クリック「チャット参照をコピー」した内容を、AI チャットへ貼り付け（`path:行-行` 形式でも可）

カプセルがあるときは、その添付だけが本文コンテキストになります。無いときの既定は Ask / Edit / Agent ともカレントタブです。Agent は加えて開いているタブ一覧を載せ、他タブの全文は最初から送りません。長い本文は文字上限で切り詰めます。
