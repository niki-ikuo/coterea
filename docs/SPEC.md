# Coterea 製品仕様書（Windows）

**Coterea** — The collaborative AI text editor.  
人と AI が、ともに書き、ともに創る場所。

| 項目 | 内容 |
|------|------|
| 文書 | 製品仕様（フェーズ1 / フェーズ2） |
| 読み | コテリア / koh-TEH-ree-uh |
| 名称 | [coterea-naming.md](./coterea-naming.md) |

---

## 1. プロダクト定義

| 項目 | 内容 |
|------|------|
| 名称 | **Coterea** |
| プラットフォーム | Windows 10/11（x64） |
| コンセプト | サーバーを持たない、ローカルファイル向けの **リアルタイム共同編集テキストエディタ**。フェーズ2で汎用 LLM API を接続し、チャットから文書を編集する |
| 対象 | メモ・文書・Markdown などテキストを扱う人。コード専用 IDE ではない |
| 非目標 | Coterea 運営のクラウド、アカウント必須、自社中継サーバー |

**「サーバーを必要としない」の意味**

- Coterea 側のバックエンド・中継・サインイン基盤は置かない。
- 共同編集は **端末同士の P2P**（同一 LAN を主想定）で成立させる。
- フェーズ2の AI は、利用者が用意した **外部 LLM API**（OpenAI 互換など）へ各端末から直接接続する。これは Coterea のサーバーではない。
- オフラインでも、単体編集と（LAN 内の）共同編集は動く。AI だけオンライン（またはローカル LLM）が必要。

---

## 2. フェーズ分割

| フェーズ | 名前 | ゴール |
|----------|------|--------|
| **フェーズ1** | テキストの共同編集 | 複数ファイルをタブで開き、プレーンテキスト / Markdown 等を編集し、**サーバーなしでリアルタイム共同編集**できる |
| **フェーズ2** | AI でのテキスト共同編集 | 右ペインのチャットで汎用 AI と対話し、提案を文書へ適用する。適用結果は **同じ共同編集セッション上の全員に見える** |

フェーズ1で右ペインは「共同編集」（参加者）。フェーズ2で同じ右ペインに AI チャットを載せる。左がエディタであることは両フェーズで変えない。

---

## 3. 画面構成

最終形（フェーズ2）:

```
┌─────────────────────────────────────────────────────────────┐
│  メニュー  [ファイル] [編集] [表示] [ヘルプ]                   │
├──────────────────────────────────┬──────────────────────────┤
│  タブ: notes.md ●  memo.txt      │  AI チャット             │
│──────────────────────────────────│  （フェーズ1は参加者）   │
│                                  │  ┌────────────────────┐  │
│         テキストエディタ         │  │ 会話 / 参加者一覧  │  │
│         （左ペイン）             │  └────────────────────┘  │
│                                  │  [入力欄]  [送信]        │
├──────────────────────────────────┴──────────────────────────┤
│  行:列 | 言語 | 共同編集: 接続 n人 | AI: 未設定 / 接続中      │
└─────────────────────────────────────────────────────────────┘
```

| 項目 | 仕様 |
|------|------|
| 既定サイズ | 1280×800、リサイズ可 |
| レイアウト | **2ペイン**（左エディタ / 右の共同編集パネル）。境界ドラッグで幅変更。右パネルは **初期非表示**。表示状態は次回起動に引き継ぐ |
| タブ | 左ペイン上部。Cursor 風のフラットなタブ（ファイル種別アイコン、ファイル名、ホバーで閉じる、未保存は閉じる位置のドット）。Markdown の切替はタブ直下のバー |
| 右ペイン（フェーズ1） | セッション状態、自分の表示名、参加者（名前・色・カーソルファイル）。同一LANは自動接続。画面右上に **このファイルの編集人数**（CodiMD 風の重なりアバター）。クリックで名前と色 |
| 右ペイン（フェーズ2） | 上記の要約（人数など）＋ **AI チャット**（履歴・入力・送信）。共同編集の詳細は右ペイン |
| ステータスバー | 行/列、言語、Markdown表示、文字コード、共同編集の接続人数、AI 設定の有無 |

フォルダツリー・Git・クリップ用の左サイドバーは **本仕様の対象外**（後続）。ファイルは「開く」ダイアログ、エクスプローラーの「Coterea で開く」、タブで扱う。

---

## 4. フェーズ1 — テキストの共同編集

### 4.1 エディタ

| 機能 | 仕様 |
|------|------|
| エンジン | Monaco Editor（VS Code と同系） |
| 操作 | 新規・開く・保存・名前を付けて保存・閉じる、Undo/Redo、検索・置換。表示倍率は **Ctrl+＋ / Ctrl+− / Ctrl+0**（JIS の `;+` キー、テンキー、左右 Ctrl いずれも可） |
| 対象ファイル | テキストとして開くもの。ダイアログは **テキスト / Windows / コード / すべてのファイル**。拡張子から Monaco の言語を付け、無いものはプレーンテキスト。エクスプローラーの「Coterea で開く」は種類を問わない |
| ハイライト例 | 文書: `.txt` `.md` `.rst` `.log` `.csv`。Windows: `.bat` `.cmd` `.vb` `.vbs` `.ps1` `.reg` `.ini` `.xaml` `.sln` `.csproj`。コード: `.js` `.ts` `.py` `.cs` `.cpp` `.html` `.json` `.xml` ほか。`hosts` や `Dockerfile` など拡張子なしも一部対応 |
| エンコーディング | UTF-8（BOM なし）を既定。他エンコーディングはステータスバーから切替可 |
| タブ | 複数ファイル同時。アクティブタブが左ペインの編集対象 |
| Markdown | `.md` / `.markdown` / `.mdx` はタブ直下で **編集 / 分割 / プレビュー** を切替。分割は左がソース・右がプレビューで、境界ドラッグで幅を変えられる。縦位置は **見出し（章）単位** で同期する（既定オン）。プレビューは共同編集の内容を追従する。既定は左右分割 |
| テーマ | **Windows ライト**（既定）、Windows ダーク、ハイコントラスト ライト / ダーク、コテリア ライト / ダーク。アプリ全体とエディタに適用。設定に保存する |
| 保存 | 単独編集時は明示保存。共同編集中は **デバウンス自動保存**（例: 500–1500ms）でローカルファイルへ反映し、他者の編集もディスクに書く |
| 閉じる / 終了 | 未保存（共同編集の未フラッシュ含む）なら確認 |

バイナリ・Office・PDF は開かない（OS 既定アプリへは後続）。

### 4.2 ファイルとタブ

| 機能 | 仕様 |
|------|------|
| 開く | ファイル選択ダイアログ。複数選択可。既に開いているパスは既存タブへ切替。エクスプローラーの右クリック **「Coterea で開く」**（インストーラが登録。既定アプリにはしない） |
| 新規 | 未保存バッファ。初回保存でパスが付く |
| 最近使ったファイル | メニューに数件（任意だが推奨） |
| 文字数 | 1ファイルあたり目安 **2MB / 約 10 万行未満** を動作保証の上限とする（超える場合は警告） |

### 4.3 リアルタイム共同編集（サーバーなし）

**モデル:** 各テキストファイルは **CRDT 文書**（推奨: Yjs）。エディタ操作は CRDT に変換し、ピアへ送る。同時編集でも上書き戦争にしない。

| 項目 | 仕様 |
|------|------|
| 同期単位 | **ファイルごと**。**同一の実体**を開いているタブだけ同期する（フルパス・ファイル名だけ・UNC・マップドライブなど表記が違っても同一視）。無題バッファと、相手が開いていないファイルは共有しない |
| 接続 | **P2P**。Coterea のクラウドは使わない |
| 発見（主） | 同一 LAN の UDP プレゼンス。一人のときは共同編集セッションを組まない |
| 参加 | 同一 LAN の Coterea を自動検出して接続。招待コードは使わない |
| シグナリング | 自前サーバーは置かない。LAN 内のピア探索で接続情報を交換する |
| 伝送 | TCP（LAN）。ファイル同期はその上に載せる |
| プレゼンス | 参加者の表示名、色、キャレット位置、選択範囲を他者のエディタに表示 |
| 権限 | フェーズ1は全員が編集可（読み取り専用は後続） |
| ホスト | **先にいた人（最古参）がハブ**。2人目は参加側。ハブは単一の TCP 中継点（仕様）。切断時は残りの最古参が引き継ぐ。一人に戻ったらハブは止める。切れ目の再接続は仕様どおり |
| 競合 | 接続中の同時編集は CRDT で自動解消。意味的な文のぶつかりはユーザーが目で直す |
| オフライン | ピア不在時は通常のローカル編集。再接続時は正本の全文スナップショットで Y.Doc を合わせる（独立に初期化した文書はマージしない）。接続後は増分同期 |

**共有の流れ（フェーズ1）**

1. 1人目はファイルを開いて普通に編集する（共同編集セッションは組まない）
2. 同一 LAN にもう1人 Coterea を開くと、**先にいた人がハブ**になり自動接続する
3. **同じ実体のファイル**を双方が開いているときだけ、そのファイルが同期される
4. ハブが終了しても、残っている最古参がハブを引き継ぐ。最後の1人になったらハブは止まる

#### 4.3.1 ファイル同一性（検討の結論）

同期の単位は **ファイル名ではなく、同じ実体を開いていること**。パス文字列の一致は用いない。無題バッファは共有しない。相手が開いていないファイルは各端末のローカル編集のまま。片方は「名前を付けて保存」で自分のディスクへコピーできる。

**同一とみなす**

- フルパス、ファイル名だけ、`\\コンピュータ名\...`、マップドライブ、`subst`、`\\?\` など、**表記が違っても同じファイル実体**
- 自ホストの管理シェア（`\\自分\c$\...`）と `C:\...`
- ハードリンクなど、同じボリューム上の同じファイルインデックス

**同一とみなさない**

- 別 PC 上の同名ローカルコピー（どちらも `C:\work\a.txt` でも別物）
- 無題同士
- 判定に自信がないとき（誤って混ぜない）

**正本の判定（優先順）**

| 順 | 手段 | 役割 |
|----|------|------|
| 1 | 開いたハンドルが返す最終パス（Windows: `GetFinalPathNameByHandle` とデバイス名解決） | マップドライブ・UNC・拡張パスなどの表記を一系統にする |
| 2a | ローカルファイル: ホスト識別 + ボリューム + ファイルインデックス | 同じディスク上の同じエントリ |
| 2b | ネットワークファイル: 正規化 UNC（ホストは短い名前の小文字、IPv4 はそのまま。自ホストならローカルへ戻す。自 NIC の IP もエイリアス） | 共有上の同じパス |
| 3 | 上記で鍵が重ならない | **同期しない** |

`net share` の一覧は、自ホストの共有名エイリアス用の補助に限る。`net use` / `subst` の文言パースは使わない（ドライブの展開は `QueryDosDevice`）。識別子が一つも取れないタブは同期しない。文書の正本 ID（Yjs）は、そのファイルを先に開いていた側（最古参）に合わせる。

**実装（Windows）**

Main が対象を `CreateFile` で開き、`GetFinalPathNameByHandle`・`QueryDosDevice`・`GetFileInformationByHandle`・`GetDriveType` で識別子を作る。Renderer はパス文字列で照合しない。

**採用しない（フェーズ1）**

| 案 | 判断 | 理由 |
|----|------|------|
| ベース名一致（`notes.md` なら同期） | 不採用 | 別場所の同名ファイルまで混ざる |
| フォルダに永続 UUID | 不採用 | フォルダコピーで別実体が同一になる。Git を汚す |
| 開いている間だけの隣ファイル（Word の `~$` 型。誰もいなければ消す） | 不採用 | 表記ゆらぎは最終パスで足りる。自動合流では残置マーカーの誤信が危険。書けない場所では使えず正本が二重になる。「最後の 1 人が消す」は P2P で脆い |

二重シェア名・DFS など、最終パスでも足りない事例が実地で出たら、一時マーカーは **補助キー** として再検討する。正本にはしない。

#### 4.3.2 同期プロトコル（実装）

Hub（Main）は Yjs を解釈しない TCP 中継。開いているファイルの照合は **`files-manifest`**（`fileIds` の重なり）。別系統の DocMeta ブロードキャスト（`collab:setDocs`）は持たない。

| メッセージ | 用途 |
|------------|------|
| `yjs` | 接続後の増分 CRDT |
| `yjs-sync` / `yjs-sync-request` | 正本の全文スナップショット。参加・正本確立時に Y.Doc を置き換える |
| `file-canonical` | 最古参が正本 `docId` を宣言する |
| `files-manifest` | 開いている実体の識別子一覧 |
| `awareness` | キャレット・選択。`user.peerId` を載せる |
| `peer-left` | ハブが全員へ中継。**離脱したピアの Awareness だけ消す**（残りのカーソルは残す） |
| `save-request` / `file-saved` | ディスクへ書くのは正本側一人。`file-saved` は `mtimeMs` / `size` を付け、他端末の外部変更検知を抑止する |

外部エディタなど本当にディスクが変わったときだけ確認ダイアログを出す。共同編集の自動保存エコー、保存待ち中、ディスク内容が直前の保存内容と一致する場合は出さない。

#### 4.3.3 テスト

共同編集の核（ファイル同一性、ハブ選出、フレーム、Awareness のピア紐づけ、外部変更の判定）は `npm test` の単体テストで固定する。LAN 2 台の E2E は手動確認（手順: [collab-lan-e2e.md](./collab-lan-e2e.md)）。

### 4.4 フェーズ1 の設定

| 項目 | 既定 |
|------|------|
| 表示名 | OS のユーザー名（変更可） |
| 表示テーマ | **Windows ライト**（既定）。Windows ダーク、ハイコントラスト、コテリア配色に切替可 |
| キャレット色 | 参加者ごとに自動割当 |
| 共同編集 | 同一 LAN で自動。一人のときは未接続 |
| 共同編集パネル | **非表示**（表示メニューまたはパネルの「非表示」。次回起動に引き継ぐ） |

### 4.5 フェーズ1 完了条件

- [ ] テキスト / Markdown / 一般的なソースをタブで複数開き、シンタックスハイライト付きで単独編集・保存できる
- [ ] Markdown をタブ直下で編集 / 分割 / プレビュー切替でき、分割幅と見出し（章）単位の位置同期を使える
- [ ] Windows ライト（既定）・ダーク、ハイコントラスト、コテリア配色を切り替えられる
- [ ] 同一 LAN の 2 台以上で、**同じ実体のファイル**を同時編集し、数秒以内に双方へ反映される（パス表記が違っても可。別場所の同名は同期しない）
- [ ] 同時に離れた箇所を編集しても、内容が壊れない（CRDT）
- [ ] 他者のキャレットが見える。誰かが抜けても残っている人のキャレットは消えない
- [ ] Coterea 独自サーバーを起動しなくても共同編集できる
- [ ] インストーラ（NSIS）で Windows に入れられる
- [ ] `npm test` が通る

---

## 5. フェーズ2 — AI でのテキスト共同編集

フェーズ1のエディタと P2P 共同編集は維持する。AI は **もう一人の共同編集者**として文書に入る（実際の書き込みは、提案をユーザーが承認したあと CRDT 経由）。

### 5.1 AI チャット（右ペイン）

アクティブファイルをコンテキストに載せ、差分プレビューしてから適用する。Agent ループ・ターミナル・ツール実行は **対象外**。

| 機能 | 仕様 |
|------|------|
| 入力 | マルチライン。Enter 送信 / Shift+Enter 改行 |
| コンテキスト | ①アクティブタブの全文 ②選択範囲があればそれを優先して明示 |
| ストリーミング | トークン単位で表示 |
| 履歴 | 端末ローカル（アプリ設定ディレクトリ）。ワークスペースサーバーは無いので **端末ごと**。共同編集相手とチャット履歴は共有しない（文書だけ共有） |
| モード | **Ask**（説明のみ、文書を変えない） / **Edit**（変更案を出し、承認後に適用） |

コンテキスト例:

```
[現在のファイル: notes.md]
（本文）

[選択範囲]
（あれば）

[ユーザー]
導入を短くして
```

### 5.2 提案の適用と共同編集

AI の Edit は **ローカルでプレビュー → ユーザー承認 → CRDT に適用** する。承認した端末から P2P で他者へ届く。AI がピアに直接書き込まない。

| 手順 | 仕様 |
|------|------|
| 1. プレビュー | 追加=緑、削除=赤の差分 |
| 2. 適用 | ファイル全体置換、または選択範囲置換（フェーズ2 MVP はこの2つ） |
| 3. 拒否 | 文書は不変 |
| 同時編集 | プレビュー生成中に他者が編集した場合は、適用前に差分を再計算するか、衝突を警告してやり直す |

Ask 中も他者は編集してよい。

### 5.3 LLM 接続（利用者の API）

Coterea はモデルをホストしない。

| 項目 | 既定 |
|------|------|
| 方式 | OpenAI 互換 Chat Completions（SSE） |
| プリセット | OpenAI / Gemini / DeepSeek / Groq / OpenRouter / **Ollama（ローカル）** / カスタム Base URL |
| API Key | 端末内で暗号化保存。Renderer から直接外部へ出さない（Main 経由） |
| モデル | プリセット候補 + 自由入力。既定例 `gpt-4o-mini` |
| 温度 | 0.2 |
| Max tokens | 8192（設定で変更可） |

Ollama 等のローカル LLM なら、AI もインターネットなしで使える。

### 5.4 フェーズ2 完了条件

- [ ] API Key を設定し、右ペインでストリーミングチャットできる
- [ ] Ask でアクティブファイルについて質問できる
- [ ] Edit の差分を承認すると、左の文書が更新される
- [ ] 共同編集中に一方が AI 適用すると、他方のエディタにも同じ変更が入る
- [ ] Key 未設定でもフェーズ1の編集・共同編集は使える

---

## 6. 技術スタック（推奨）

Windows デスクトップと Monaco を優先する。

| 層 | 技術 |
|----|------|
| UI | React + TypeScript |
| シェル | Electron 33+ |
| エディタ | Monaco Editor |
| 共同編集 | **Yjs** + Monaco バインディング |
| P2P | LAN は UDP 発見 + TCP 中継（フェーズ1）。WebRTC は後続オプション |
| 状態 | Zustand |
| ビルド | electron-vite |
| 配布 | electron-builder（NSIS） |
| AI | Main プロセスから fetch + SSE |

**Electron を選ぶ理由:** Monaco との相性、Windows パッケージの成熟、MVP 速度。

**後続の代替:** Tauri はバイナリは小さいが、Monaco + P2P のコストが高い。フェーズ1–2では採用しない。

---

## 7. アーキテクチャ

```
┌─────────────────────────────────────────────────┐
│                 Renderer                         │
│  ┌──────────────┐     ┌─────────────────────┐   │
│  │ Monaco+Yjs   │     │ 右ペイン             │   │
│  │ タブ / 編集  │     │ P1: 共同編集         │   │
│  └──────┬───────┘     │ P2: AI チャット      │   │
│         │             └──────────┬──────────┘   │
│         └────────────┬───────────┘               │
│                   Zustand                        │
└──────────────────────┬──────────────────────────┘
                       │ IPC
┌──────────────────────┴──────────────────────────┐
│                   Main                           │
│  fs / ファイル同一性  │  P2P（LAN TCP）  │  settings  │  AI  │
└─────────────────────────────────────────────────┘
        │  LAN ピア（サーバーなし）
        ▼
   他の Coterea 端末
```

### IPC（最小）

| チャンネル | 方向 | 用途 |
|------------|------|------|
| `fs:open` / `read` / `write` / `saveAs` / `identity` / `stat` | R→M | ファイル。`read` / `identity` は実体識別子 `fileIds` を返す。`stat` は共同編集の保存エコー用 |
| `collab:enable` / `startHost` / `join` / `leave` | R→M | セッション。開いているファイルは `files-manifest` フレームで共有する（`setDocs` は無い） |
| `collab:send` / `collab:frame` | 双方向 | CRDT・Awareness・保存協調などの制御フレーム |
| `settings:get` / `set` | R→M | 表示名等 |
| `ai:chat`（フェーズ2） | R→M | SSE ストリーム |

Renderer は生のソケットや API Key を持たない。

---

## 8. データモデル（最小）

```typescript
interface OpenTab {
  id: string;
  path: string | null; // 未保存は null
  fileIds: string[]; // 実体の識別子。無題は空。重なれば同一ファイル
  language: string;
  ydoc: unknown; // Y.Doc
  isDirty: boolean;
}

interface Peer {
  id: string;
  displayName: string;
  color: string;
}

interface CollabSession {
  roomId: string;
  peers: Peer[];
  role: "host" | "guest";
}

interface AppSettings {
  displayName: string;
  theme: "win-light" | "win-dark" | "hc-light" | "hc-dark" | "warm-light" | "warm-dark";
  // フェーズ2
  providerId?: string;
  apiBaseUrl?: string;
  model?: string;
  temperature?: number;
}
```

API Key は設定オブジェクトに平文で載せない（Main の安全なストア）。

ローカル保存の目安:

```
%APPDATA%/Coterea/
  settings.json      # Key 以外
  secrets.bin        # 暗号化 Key（フェーズ2）
  chat-history.json  # フェーズ2
  recent-files.json
```

共同編集の CRDT 本体は、保存時にプレーンテキストへ落としてユーザーのファイルに書く。独自の必須クラウド領域は作らない。

---

## 9. 非機能要件

| 項目 | 目標 |
|------|------|
| 起動 | コールドスタート 5 秒以内（目安） |
| 共同編集遅延 | 同一 LAN・小文書で往復 **500ms 未満**を目標 |
| 同時人数 | フェーズ1は **2〜4人** を保証。それ以上は最善努力 |
| メモリ | 小文書数タブで 400MB 未満を目安 |
| インストーラ | NSIS、スタートメニュー |
| セキュリティ | Key は平文保存しない。共同編集は同一 LAN 上の Coterea 同士。公開 Wi-Fi は非推奨と明記 |
| オフライン | 編集は可。共同編集は LAN 内ピアがいれば可。クラウド AI は不可（Ollama は可） |
| アクセシビリティ | タブ・ボタンはキーボード操作可（最低限） |

---

## 10. 開発フェーズと期間目安（1人想定）

| 段階 | 内容 | 目安 |
|------|------|------|
| P1-a | Electron、Monaco、タブ、開く/保存 | 1 週 |
| P1-b | Yjs をローカル文書に接続、Undo と保存 | 1 週 |
| P1-c | LAN 発見 + P2P + awareness | 2 週 |
| P1-d | インストーラ、切断・再接続、基本テスト | 1 週 |
| **フェーズ1 合計** | | **約 5 週** |
| P2-a | 設定（プロバイダ・Key）、右ペインチャット SSE | 1 週 |
| P2-b | Ask / Edit、差分プレビュー、CRDT へ適用 | 1 週 |
| P2-c | 共同編集中の AI 適用・衝突、テスト | 3〜5 日 |
| **フェーズ2 合計** | | **約 2.5 週** |

---

## 11. 後続（本仕様の外）

次は含めない。必要になったら別仕様にする。

- フォルダワークスペース、ファイルツリー、全文検索、Git
- インターネット越えのリレーサーバー（Coterea 運営しない。利用者が自分のシグナリングを置くのは将来オプション）
- Agent / ターミナル / MCP / RAG
- 読み取り専用権限、コメントスレッド、バージョン履歴 UI
- モバイル / macOS / Linux

---

## 12. まとめ

Coterea は Windows 上の **左エディタ・右パネル** のテキストアプリである。

1. **フェーズ1** — タブで複数のテキストを開き、**サーバーなし P2P** でリアルタイム共同編集する。
2. **フェーズ2** — 利用者が繋いだ汎用 LLM とチャットし、承認した編集を **同じ CRDT 文書** に載せて全員で共有する。

本質は「ローカルファイル + リアルタイムな人の共同編集 +（任意の）AI という共同編集者」である。
